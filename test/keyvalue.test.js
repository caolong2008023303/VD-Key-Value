var chai = require('chai');
var app = require('../app');
var expect = chai.expect;

// :todo
describe('KeyValues API', function () {
    it('Should complete this test further', function () {
        return new Promise(function (resolve, reject) {
            expect(true).to.be.true;
            resolve();
        });
    });
});